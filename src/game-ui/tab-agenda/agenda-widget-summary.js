const { VerticalBox } = require("../../wrapper/api");

/**
 * Agenda phase summary, used for main UI (streamer request).
 * Can handle
 */
class AgendaWidgetSummary extends VerticalBox {}

module.exports = { AgendaWidgetSummary };
